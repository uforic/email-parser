schema:
    - schema.graphql
generates:
    src/graphql/__generated__/resolvers.ts:
        plugins:
            - typescript
            - typescript-resolvers
        config:
            useIndexSignature: true
